interface Vector1D {
  x: number;
}
interface Vector2D {
  x: number;
  y: number;
}
interface Vector3D {
  x: number;
  y: number;
  z: number;
}

// extneds 를 사용할때와 비교하여
// 할당 가능, 서브타입, 집합의 관계가 바뀌지 않는다.
